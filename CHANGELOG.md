# 修改记录

## [1.6.0] - 2026-09-02

### 新增

- 产品源增加 HTTPS 自定义源。
- 自定义源按“目录1”至“目录5”逐层按需发现，找到 `repodata/repomd.xml` 后作为最终仓库解析。
- 兼容阿里云镜像目录中的站外导航链接和自身链接。
- 搜索结果下载地址支持双击选择和按钮复制。

### 变更

- 软件仓库由多选列表改为单选下拉，一次仅搜索一个仓库。
- 软件仓库显示在芯片架构之前，使选择顺序符合仓库 URL 层级。
- 自定义源采用逐层加载，不全树遍历大型历史镜像。

### 修复

- 修复从少量 RPM 仓库切换到大量仓库后仍显示旧结果的问题。
- 切换产品源、目录、组件、软件仓库或架构时立即清空结果表。
- 修复镜像目录含站外链接时整个扫描中止的问题。
- 修复自定义源继承上一来源架构选项的问题。
- 软件和 Debian 包版本更新为 `1.6.0`。

## [1.5.0] - 2026-08-31

### 新增

- EPKL multi_version 组件从远程目录动态发现。
- 增加“组件子目录”和“扩展子目录”动态下拉，支持 ContainerTools 的模块版本层级。
- 自动识别动态目录中的架构层。
- 最终仓库 RPM 数量少于等于 30 个时自动显示全部包。

### 内存优化

- 缓存升级为版本化 gzip JSON Lines，逐条读写而非整份 `json.load`。
- primary XML 使用 `iterparse` 并及时清理节点。
- 搜索线程逐包读取、逐包筛选，不再构造完整仓库列表。
- 主窗口不再长期保存全部仓库包对象。
- 大型仓库禁止无条件手动搜索，避免创建海量表格行。

### 修复

- 修复切换目录时旧线程结果或错误覆盖新仓库的问题。
- 修复自动预览可能在旧 QThread 结束前启动新线程的问题。
- 修复动态架构切换后不重新预览的问题。
- 保持搜索结果按仓库、RPM 版本和包名排序。
- 软件和 Debian 包版本更新为 `1.5.0`。

## [1.4.0] - 2026-08-31

### 新增

- EPKL 源增加独立仓库分类下拉：`main`、`update`、`multi_version`。
- 选择 `multi_version` 后再显示 Compiler、DB、Storage 等维护组件。

### 安全与健壮性

- 限制 repomd、压缩元数据和解压后元数据大小，防止内存耗尽。
- 仓库、primary 和 RPM 地址强制 HTTPS，并阻止跨域与目录穿越。
- 缓存命中时重新校验 URL 边界和 RPM 摘要。
- 拒绝下载没有仓库摘要的 RPM。
- 下载进度、成功和失败信号增加控件边界检查。
- 下载失败信息按类别脱敏展示。
- 修复 RPM epoch 未写入导致版本排序不完整的问题。

### 审查说明

- 审查报告中的压缩上限、URL 越界、HTTPS、响应大小、Zstd 检查、摘要校验和 UI 防御建议成立并已处理。
- 报告所称 HPC 缺少仓库配置不成立：远程实际提供 main、update 和 multi_version，当前标准仓库回退有效。
- 未采用连接池建议，因为会增加外部包依赖，且当前标准库实现满足需求。

## [1.3.0] - 2026-08-27

### 新增

- 产品源增加 `https://update.cs2c.com.cn/CS/`。
- CS 源独立提供 V10、V10SP3/V10SP3-2403、维护组件、架构和仓库联动。
- CS 支持 `hwy`、`aiplus`、`ccw`、`gazb`、`lowlatency` 和 `kernel-4k` 实际仓库。

### 修复

- 修复 TXT 中完整 `name-version-release.arch` 无法匹配的问题。
- 完整 NEVRA 和 RPM 文件名改为精确匹配，避免 `satyr` 误命中 `python2-satyr`。
- 保持纯包名和通配符的模糊搜索行为。
- 软件、网络请求标识和 Debian 包版本更新为 `1.3.0`。

## [1.2.0] - 2026-08-27

### 变更

- “产品源”改为实际仓库来源：“系统源”和“EPKL 源”。
- 系统源固定使用 `https://update.cs2c.com.cn/NS/`。
- EPKL 源固定使用 `https://eps-server.openkylin.top/NS/`。
- 两类来源分别提供独立的系统版本、发行版本、维护组件、架构和软件仓库选项。
- 系统源仅显示 `base/update`，不再混入 EPKL 仓库。
- EPKL 源显示 `main/update`，并按实际远程结果提供 `multi_version` 组件。
- 增加 EPKL 的 HPC、V10.4-HPC 和 V10AIPLUS 等实际发行分类。
- 软件、网络请求标识与 Debian 包版本更新为 `1.2.0`。

### 修复

- 修复将产品名称与仓库来源混用导致下级选项不准确的问题。
- 修复系统源和 EPKL 源使用同一套发行版、组件与架构选项的问题。
- 根据实际 `repomd.xml` 探测结果收紧 multi_version 组件，避免产生 404 仓库地址。

## [1.1.0] - 2026-08-26

- 增加 V10/V11、发行版本号和维护组件联动。
- 增加暂停/恢复下载、上下可调结果窗格和多尺寸任务栏图标。
- 支持 V11 Zstandard 元数据。

## [1.0.0] - 2026-08-26

- 新增 TXT 包名导入、仓库索引缓存和多线程下载。
- 结果按仓库、RPM 版本、包名排序。

## [0.2.0] - 2026-08-26

- 项目更名并统一包名、安装路径和启动命令。

## [0.1.0] - 2026-08-26

- 实现 PyQt5 RPM 仓库选择、搜索与批量下载基础功能。
